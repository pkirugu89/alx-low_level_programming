#include <stdio.h>
/*
 * main-Prints all digits from 1  to 10.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		printf("%d", x);
	}

	putchar('\n');

	return (0);
}
