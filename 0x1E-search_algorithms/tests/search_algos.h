#ifndef SEARCH_ALGOS
#define SEARCH_ALGOS

/* C Library imports */
#include <stdio.h>
#include <stdlib.h>

/* Task Prototypes */
/* Task 0: Linear search prototype */
int linear_search(int *array, size_t size, int value);
/* Task 1: Binary search prototype */
int binary_search(int *array, size_t size, int value);
#endif
